import { useEffect, useState, useRef } from 'react';
import { useFrame, useThree } from '@react-three/fiber';
import * as THREE from 'three';

const PlayerController = ({ modelRef }) => {
    return (
        <></>
    )
};

export default PlayerController;